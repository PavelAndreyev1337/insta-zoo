# Insta zoom backend

The application was created for educational purposes.

## Installation
* rename `application-example.properties` to `application.properties`
* change credentials in `application.properties`
* rename `SecurityConstantsExample` from package `com.example.demo.security` to `SecurityConstants`
* change credentials in `SecurityConstants`
